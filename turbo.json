{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["WEDDING_DATABASE_URL", "GUESTBOOK_ADMIN_SECRET"]
    },
    "lint": {
      "dependsOn": ["^lint"],
      "env": ["NODE_ENV", "GUESTBOOK_ADMIN_SECRET"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
