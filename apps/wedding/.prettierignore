# ──────────────────────────────────────────────────────────
# wedding 앱 전용 Prettier 제외 목록
#
# 주의: Prettier는 .prettierignore를 병합하지 않습니다.
#   - 루트에서 실행(pnpm format)하면 루트 .prettierignore만 적용됩니다.
#   - wedding 디렉토리에서 직접 실행할 때 이 파일이 적용됩니다.
#   루트 .prettierignore에 공통 제외 경로를 추가하는 것을 권장합니다.
# ──────────────────────────────────────────────────────────

# 자동 생성된 플레이스홀더 사진 (실제 사진으로 교체 전까지 포맷 제외)
# /public/photos/

# Drizzle 마이그레이션 자동 생성 파일
# /drizzle

# Next.js 자동 생성 타입 선언 파일
# next-env.d.ts
